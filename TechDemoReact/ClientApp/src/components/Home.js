import React, { Component } from 'react';

export class Home extends Component {
  static displayName = Home.name;

  render () {
    return (
      <div>
            Basic CRUD using react and primereact library for client-side coding and ASP.NET CORE as backend. My 'Hello world' project on react            
      </div>
    );
  }
}
